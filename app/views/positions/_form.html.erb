<%= simple_form_for(@position) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :position %>
    <%= f.association :interview %>
    <%= f.association :business %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
